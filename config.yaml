proxy:

  listen:
    address: "0.0.0.0"
    port: 8081

  services:

    - name: my-service
      domain: my-service.my-company.com
      hosts:
        - address: "127.0.0.1"
          port: 9091
        - address: "127.0.0.1"
          port: 9092